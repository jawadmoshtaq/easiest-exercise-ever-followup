# Easiest Exercise Ever (Python)

Practice tasks implemented as small functions inside `src/tasks.py`.
Quick manual runs live in `src/main.py`.

## Run
```bash
python src/main.py


```bash
git add README.md
git commit -m "docs: add README"
git push
